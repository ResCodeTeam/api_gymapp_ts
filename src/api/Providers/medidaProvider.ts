/**
 * Classe que representa uma medida de uma avaliação
 * @param avaliacaoId id da avaliação
 * @param medida medida
 * @param unidadeMedida unidade da medida
 * @param localMedidaId id do local da medida
 */
export class Medida {
    avaliacaoId: string;
    medida: string;
    unidadeMedida: string;
    localMedidaId: string;
}