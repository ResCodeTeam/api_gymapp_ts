/**
 * Classe que representa as series de um plano
 * @param serieId id da série
 * @param nOrdem numero de ordem da série
 * @param valor quantidade de repetições da série
 * @param peso peso da série
 * @param unidadeMedida unidade de medida do peso
 */
export class SeriesP {
    serieId: string;
    nOrdem: number;
    valor: string;
    peso: number;
    unidadMedida: string;
}